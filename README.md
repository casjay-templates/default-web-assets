Default web assets I use on all my sites

### Install

```bash
export STATICSITE="$(hostname -f)"
sudo -HE bash -c "$(curl -LSs https://github.com/casjay-templates/default-web-assets/raw/main/setup.sh)"
```
